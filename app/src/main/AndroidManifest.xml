<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.primeravance">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MostrarVar"></activity>
        <activity android:name=".MostrarEst" />
        <activity android:name=".Variable" />
        <activity android:name=".Estatica" />
        <activity android:name=".SubneteoVar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/titulo" />
        <activity android:name=".Subneteo" />
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>